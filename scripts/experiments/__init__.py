"""Experiment runners, importable as ``experiments.<module>``.

Present only so that a script in another role directory can import a runner it
genuinely reuses (``scripts/`` is already on ``sys.path`` in every such caller).
Adds no behaviour of its own.
"""
