"""Figure/atlas renderers, importable as ``rendering.<module>``.

Present only so that a script in another role directory can import the plotting
grammar defined here (``scripts/`` is already on ``sys.path`` in every such
caller). Adds no behaviour of its own.
"""
